import React from "react";

const Loading = () => {
    return (
        <div className="font-bold text-2xl text-white absolute right-[50%] top-[50%] translate-x-[50%]">
            Loading...
        </div>
    );
};

export default Loading;
